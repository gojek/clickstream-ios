# .github/actions/build/action.yml
name: build_action
description: Build & archive Clickstream target

runs:
  using: "composite"
  steps:
    - name: Run build with SPM
      run: bundle exec fastlane spm_build
      shell: bash
  
    - name: Run build
      run: bundle exec fastlane build_target
      shell: bash